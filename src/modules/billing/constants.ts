/** Free-tier cap on groups a user may create. */
export const MAX_FREE_GROUPS = 5;

/** Expense CSV/PDF export requires Pro (same gate pattern as MAX_FREE_GROUPS). */
export const EXPORT_REQUIRES_PRO = true;
